import 'package:flutter/material.dart';

class RecipeDetailInstructions extends StatelessWidget {
  const RecipeDetailInstructions({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return RecipeDetailInstructions();
  }
}
